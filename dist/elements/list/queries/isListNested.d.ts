import { Editor, Path } from 'slate';
import { ListOptions } from '../types';
/**
 * Is the list nested, i.e. its parent is a list item.
 */
export declare const isListNested: (editor: Editor, listPath: Path, options?: ListOptions | undefined) => boolean;
//# sourceMappingURL=isListNested.d.ts.map